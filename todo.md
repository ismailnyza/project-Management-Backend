add proper db connection so no data is lost 
clean architecture
unit tests

and a proper refactor

auth via github / anything else and user name password
import tasks from ether xcal or github tasks

tui so its jira but for devs with discord intergration
